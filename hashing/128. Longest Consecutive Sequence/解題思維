***unordered_set法
1.因為重複的元素沒有用，所以使用set來消除重複的元素
2.因為要達到O(n)的時間複雜度，所以要用不用排列的unordered_set
3.對nums每個數字遍歷，偵測前後的數字有沒有連續，設prev和next為上一個和下一個的數字，如果有，計數，並每次算到就移除set內的值，避免之後重複計數
(對nums遍歷，刪除的是set，因為如果遍歷set又刪除set會錯，因為在往前往後(prev,next)的時候會偵測到已經刪掉的元素，造成存取已經刪掉的元素)


***unordered_set法精簡版
1.如上一個數不在set，表示此數為連續序列中第一個，才開始計數，其他跳過
